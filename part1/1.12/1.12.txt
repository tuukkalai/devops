Command to build image and running image on port 5000
-----------------------------------------------------

> d build . -t front-proj && d run -d -p 5000:5000 front-proj

Open browser with address localhost:5000 to view `screenshot.png`
